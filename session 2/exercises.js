// Change <script src="app.js"></script> to <script src="exercises.js"></script>
// Then solve 1 & 2.

/* Example: 
1 - Define a function called sayHello that takes a parameter called 'name'.
2 - Write the code in the body of sayHello that will print "Hello {name}" in the console.
3 - call the sayHello function twice with the following expected output:
    Hello Ana
    Hello John
*/

// Type your code solution below:

function sayHello(name) {
  console.log('Hello' + name);
}

sayHello('Ana');
sayHello('John');

/* -------------------------------------- */

/* 1: 
1 - Define a function called printSquareOf that takes a parameter called 'x'.
2 - Write the code in the body of printSquareOf that will print the square of x in the console.
3 - call the printSquareOf function twice with the following expected output:
    25
    64
*/

// Type your code solution below:

/* -------------------------------------- */

/* 2: 
1 - Create / Define two functions:
    getSquareOf: takes one parameter, & it should return the square of the parameter.
    sumOf: takes two parameters, & it should return the sum of the two parameters.
2 - If everything done correctly, once you save the file, you should see "Result: 40" in the browser's console.
*/

// Type your code solution below:

var result = getSquareOf(5) + sumOf(7, 8);
console.log('Result: ', result);

/* -------------------------------------- */
